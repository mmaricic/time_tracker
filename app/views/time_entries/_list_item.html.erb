<div id="<%= dom_id(time_entry) %>">
  <div><%= time_entry.description %></div>
  <div>
    <span>start time:</span>
    <span><%= time_entry.start_time.strftime("%H:%M:%S") %></span>
  </div>
  <div>
    <span>end time:</span>
    <span><%= time_entry.end_time.strftime("%H:%M:%S") %></span>
  </div>
  <div>
    <span>total time:</span>
    <span><%= pp_total_time(time_entry.total_time) %></span>
  </div>
   <div>
  <%= link_to "Edit", edit_time_entry_path(time_entry) %>
  </div>
</div>