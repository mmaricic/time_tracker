<%= form_with scope: :time_entry, url: time_entries_path, remote: true, html: { id: :start_timer_form, style: "display: #{hidden ? 'none' : 'block'}" } do |f| %>
  <%= f.text_field :description %>
  <%= f.hidden_field :start_time %>
  <%= f.submit "Start", id: :start_timer_btn %>
<% end %>